#include <stdio.h> // vai ser usado em todos os códigos, serve para entrada e saída de dados
#include <stdlib.h> // quando o programa for executado pelo windows ele não ser fechado automaticamente

int main(){
    printf("Hello, World!\n"); // não esquecer dos ponto e vírgula
    system("pause"); // função para pausar o programa
    return 0; // não é obrigatório mas é recomendado deixar explicito
}